query Videos($limit: Int!, $start: Int, $where: JSON) {
  videos(limit: $limit, start: $start, where: $where) {
    id
    url
    thumbnailUrl
    title
    description
    tags {
      id
      name
    }
  }
}
